# Agent Core

Shared Python package for:

- domain models
- multimodal parsing
- RAG chunking and retrieval
- multi-agent workflow
- model fine-tuning data structures
- evaluation metrics

The first implementation is a scaffold. Replace placeholder classes with production integrations incrementally.
