# Models Directory

Use this directory for local fine-tuning artifacts.

```text
checkpoints/  LoRA, DPO, reranker, and SFT checkpoints
outputs/      Evaluation outputs and generated samples
```

Model artifacts are ignored by Git.
